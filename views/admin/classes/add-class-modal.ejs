<!-- Modal for adding class -->
<div id="add-class-modal" class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-50 transition-opacity duration-300 ease-out opacity-0 pointer-events-none">
    <div class="bg-white rounded-lg shadow-xl w-full max-w-lg overflow-hidden transition-all duration-300 ease-out transform scale-95 opacity-0">
        <div class="px-6 py-4 bg-gray-50 border-b border-gray-200 flex justify-between items-center">
            <h3 class="text-lg font-semibold text-gray-800">Create New Class</h3>
            <button id="close-add-class-modal" class="text-gray-400 hover:text-gray-600 focus:outline-none">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
        </div>
        <!-- Class creation form -->
        <form action="/admin/classes/create" method="POST" class="px-6 py-6 space-y-4">
            <div>
                <label for="name" class="block text-sm font-medium text-gray-700">Class Name *</label>
                <input type="text" id="name" name="name" value="<%= typeof values !== 'undefined' ? values.name || '' : '' %>" required class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm">
            </div>
            <div>
                <label for="description" class="block text-sm font-medium text-gray-700">Description</label>
                <textarea id="description" name="description" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm resize-vertical h-24"><%= typeof values !== 'undefined' ? values.description || '' : '' %></textarea>
            </div>
            <div>
                <label for="teacher" class="block text-sm font-medium text-gray-700">Teacher</label>
                <select id="teacher" name="teacherId" class="mt-1 block w-full px-3 py-2 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm">
                    <option value="">Select Teacher</option>
                    <% if (typeof teachers !== 'undefined' && teachers.length > 0) { %>
                        <% teachers.forEach(teacher => { %>
                            <option value="<%= teacher._id %>" <%= typeof values !== 'undefined' && values.teacherId === teacher._id ? 'selected' : '' %>>
                                <%= teacher.name %> (<%= teacher.email %>)
                            </option>
                        <% }); %>
                    <% } %>
                </select>
            </div>
            <div class="pt-2">
                <button type="submit" class="w-full inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition duration-150 ease-in-out">
                    Create Class
                </button>
            </div>
        </form>
    </div>
</div>

<script>
    const addClassModal = document.getElementById('add-class-modal');
    const addClassModalContent = addClassModal.querySelector('.bg-white');

    function showAddClassModal() {
        addClassModal.classList.remove('pointer-events-none');
        addClassModal.classList.remove('opacity-0');
        addClassModalContent.classList.remove('opacity-0', 'scale-95');
        addClassModalContent.classList.add('opacity-100', 'scale-100');
    }

    function hideAddClassModal() {
        addClassModal.classList.add('opacity-0');
        addClassModalContent.classList.add('opacity-0', 'scale-95');
        addClassModalContent.classList.remove('opacity-100', 'scale-100');
        setTimeout(() => {
            addClassModal.classList.add('pointer-events-none');
        }, 300); // Match duration-300
    }

    // Trigger to show the modal (assuming a button with id 'add-class-button' exists elsewhere, e.g., in index.ejs)
    // Ensure the button exists and has the correct ID
    const addClassButton = document.getElementById('add-class-button');
    if (addClassButton) {
        addClassButton.addEventListener('click', function(e) {
            e.preventDefault();
            showAddClassModal();
        });
    }

    document.getElementById('close-add-class-modal').addEventListener('click', hideAddClassModal);

    // Close modal on background click
    addClassModal.addEventListener('click', function(e) {
        if (e.target === this) {
            hideAddClassModal();
        }
    });
</script>