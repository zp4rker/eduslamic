<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'eduslamic' %></title>
    <link rel="stylesheet" href="/styles/tailwind.css">
</head>
<body class="bg-gray-50 min-h-screen">
    <div id="page-wrapper" class="transition-all duration-300">
        <%- include('../partials/navbar', typeof navbar !== 'undefined' ? navbar : {}) %>
        <main>
            <%- body %>
        </main>
    </div>
</body>
</html>
