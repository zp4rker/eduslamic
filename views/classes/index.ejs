<div class="max-w-5xl mx-auto mt-8 px-4">
    <%- include('../partials/alerts', { success: success, error: error }) %>
    <div class="bg-white border border-gray-200 rounded-lg shadow-md p-8 mb-8 space-y-6">
        <h2 class="text-xl font-semibold">Class Management</h2>
        <a href="/classes/create" class="mt-2 inline-block px-4 py-2 rounded bg-green-600 text-white hover:bg-green-700 transition-colors duration-150">Add New Class</a>
        <% if (classes && classes.length > 0) { %>
            <div class="overflow-x-auto mt-6">
                <table class="min-w-full bg-white border border-gray-200 rounded-lg">
                    <thead>
                        <tr class="bg-gray-100">
                            <th class="px-4 py-2 text-left font-medium text-gray-700">Name</th>
                            <th class="px-4 py-2 text-left font-medium text-gray-700">Description</th>
                            <th class="px-4 py-2 text-left font-medium text-gray-700">Teacher</th>
                            <th class="px-4 py-2 text-left font-medium text-gray-700">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% classes.forEach(classObj => { %>
                            <tr class="border-t border-gray-100 hover:bg-gray-50">
                                <td class="px-4 py-2"><%= classObj.name %></td>
                                <td class="px-4 py-2"><%= classObj.description || 'No description' %></td>
                                <td class="px-4 py-2"><%= classObj.teacherName %></td>
                                <td class="px-4 py-2">
                                    <div class="flex flex-wrap gap-2">
                                        <a href="/classes/view/<%= classObj._id %>" class="px-3 py-1 rounded bg-blue-600 text-white hover:bg-blue-700 transition-colors duration-150">View</a>
                                        <a href="/classes/edit/<%= classObj._id %>" class="px-3 py-1 rounded bg-yellow-500 text-white hover:bg-yellow-600 transition-colors duration-150">Edit</a>
                                        <a href="/classes/<%= classObj._id %>/students" class="px-3 py-1 rounded bg-green-600 text-white hover:bg-green-700 transition-colors duration-150">Manage Students</a>
                                        <form action="/classes/delete/<%= classObj._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this class?');" class="inline">
                                            <button type="submit" class="px-3 py-1 rounded bg-red-600 text-white hover:bg-red-700 transition-colors duration-150">Delete</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <p class="text-gray-600">No classes found. Add a new class to get started.</p>
        <% } %>
    </div>
</div>